# Copy to .env and adjust as needed

# Set to false when your Java backend is running at localhost:8080
VITE_USE_MOCK=true

# Override if backend is deployed elsewhere
VITE_API_BASE=http://localhost:8080
